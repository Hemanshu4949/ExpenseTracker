<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ExpenseTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ExpenseTracker" parent="Base.Theme.ExpenseTracker" />

    <style name="PaymentMethodButton" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <!-- Inherit from UnelevatedButton, which has no default border -->
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <!-- Apply our custom selector as the background -->
        <item name="android:background">@drawable/payment_button_selector</item>
        <!-- CRITICAL: Set backgroundTint to null. This prevents the button's default
             background color from being drawn on top of our custom drawable. -->
        <item name="backgroundTint">@null</item>
        <item name="android:textColor">@color/add_expense_text_light</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:iconTint">@color/add_expense_text_medium</item>
        <item name="android:gravity">center_vertical|start</item>
        <item name="android:paddingStart">16dp</item>
    </style>

</resources>